# 다음 작업을 위한 인계

## 목표와 내가 정한 조건

- 끝내려는 일: 9/23 발표 준비. "자료 근거를 확인하는 학습 도우미" 주제로 NotebookLM ↔ Perplexity 비교 + 개선안(조사노트, 제안 단계). 준비물 `발표자료.html`(4~6화면)·`출처목록.md`를 9/22 18:00(KST)까지
- 유지할 조건과 판단:
  - 제출물은 **HTML**로 낸다 (메모의 "PDF" 표기는 HTML로 확정)
  - 직접 안 쓴 기능은 후기처럼 쓰지 않고, 미구현 개선안은 "제안 단계"로 명시
  - 손글씨 메모는 `memo-to-json` 스킬로 5항목 JSON으로 정리하고, 결과는 `submissions/`에 저장

## 현재 상태

- 실제로 끝내고 확인한 일:
  - 발표안내·조사메모 판독 및 조건/마감 정리
  - `memo-to-json` 스킬에 `scripts/save_memo.py`(검사·저장) 추가, SKILL.md에 판독 규칙·실행 절차 연결
  - 정상/오류/빈목록 입력으로 스크립트 검증 완료(오류 시 저장 안 함 확인)
  - `내준비메모.png`(9/16) → `submissions/내준비메모.json`
  - `다음메모.png`(9/20) → `submissions/다음메모.json`
  - 스킬 사본을 `submissions/skills/memo-to-json/`에 보관
- 아직 하지 않은 일: `발표자료.html`·`출처목록.md` 작성, Steering(공통 규칙) 파일 작성, 커밋·푸시
- 확인이 필요한 질문: **제출 경로**(별도 안내 미수신, 미해결 상태로 진행 중)

## 이어가기 전에 읽을 파일

- 파일과 읽어야 하는 이유:
  - `submissions/내준비메모.json`, `submissions/다음메모.json` — 진행 상황(끝낸 일/남은 일/변경/질문)
  - `data/주제조사메모.md` — 서비스 설명 근거와 출처 S1~S3
  - `data/교수님에게 전달받은 발표안내 내용.md` — 제출 요건·마감
- 사용할 스킬과 적용할 프로젝트 규칙: `memo-to-json`(손글씨 메모→JSON). 결과 JSON은 항상 `submissions/`에 저장

## 다음 행동

- 다음 세션에서 먼저 할 일:
  1. 제출 경로 확인(별도 안내 확인)
  2. NotebookLM·Perplexity 비교표 + 개선안 그림을 반영해 `발표자료.html`(4~6화면) 작성, 브라우저에서 글자 잘림·링크 점검
  3. `출처목록.md`에 S1~S3(제목·기관·URL·확인일) 정리
  4. 공통 규칙을 Steering 파일로 저장하고 실제 적용 확인
- 진행 전에 내 확인이 필요한 것: 슬라이드 분량(4쪽 확정 vs 표지·출처 감안 5~6쪽), 제출 경로
